<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <title>Comidas do <%= pais %></title>
  <style>
    body {
      font-family: Arial, sans-serif;
      padding: 40px;
      text-align: center;
      background-color: #f3f3f3;
    }
    h1 {
      margin-bottom: 30px;
      text-transform: capitalize;
    }
    .comidas {
      display: flex;
      flex-wrap: wrap;
      justify-content: center;
      gap: 30px;
    }
    .comida {
      background: #fff;
      border: 1px solid #ccc;
      border-radius: 10px;
      width: 220px;
      padding: 15px;
      box-shadow: 0 2px 8px rgba(0,0,0,0.1);
    }
    .comida img {
      width: 100%;
      height: 140px;
      object-fit: cover;
      border-radius: 8px;
    }
    .comida h3 {
      margin: 10px 0 5px;
    }
    .comida p {
      margin: 0;
      color: #555;
    }
    .voltar {
      margin-top: 30px;
      display: inline-block;
      text-decoration: none;
      color: #333;
      border: 1px solid #333;
      padding: 8px 16px;
      border-radius: 6px;
      transition: 0.3s;
    }
    .voltar:hover {
      background-color: #333;
      color: white;
    }
  </style>
</head>
<body>
  <h1>Comidas do <%= pais %></h1>
  <div class="comidas">
    <% if (foods.length === 0) { %>
      <p>Nenhuma comida encontrada para este país.</p>
    <% } else { %>
      <% foods.forEach(comida => { %>
        <div class="comida">
          <img src="<%= comida.imagem %>" alt="<%= comida.nome %>">
          <h3><%= comida.nome %></h3>
          <p>Preço: R$ <%= comida.preco.toFixed(2) %></p>
        </div>
      <% }) %>
    <% } %>
  </div>
  <a href="/" class="voltar">← Voltar</a>
</body>
</html>
